@model DVD.Web.Models.LoanTypeModel

@{
    ViewData["Title"] = "AddLoanType";
    if (ViewData["Error"] != null)
    {
        <script>
                alert("@ViewData["Error"]");
        </script>
    }
}

<h1>Add Loan Type</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("AddLoanType", "LoanType", FormMethod.Post, new { @role = "form", @id = "AddLoanType" }))
        {
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LoanType" class="control-label"></label>
                @Html.TextBoxFor(x => x.LoanType, new { @class = "form-control", @placeholder = "Loan Type" })
                <span asp-validation-for="LoanType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LoanDuration" class="control-label"></label>
                @Html.TextBoxFor(x => x.LoanDuration, new { @type="date", @class = "form-control", @placeholder = "Loan Duration" })
                <span asp-validation-for="LoanDuration" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
                <a asp-action="Index" class="btn btn-outline-danger">Back to List</a>
            </div>
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}