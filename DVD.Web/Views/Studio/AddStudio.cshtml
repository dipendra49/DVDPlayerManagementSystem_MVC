@model DVD.Web.Models.StudioModel

@{
    ViewData["Title"] = "AddStudio";
    if (ViewData["Error"] != null)
    {
        <script>
                alert("@ViewData["Error"]");
        </script>
    }
}

<h1>Add Studio</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("AddStudio", "Studio", FormMethod.Post, new { @role = "form", @id = "AddStudio" }))
        {
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudioName" class="control-label"></label>
                @Html.TextBoxFor(x => x.StudioName, new { @class = "form-control", @placeholder = "Studio Name" })
                <span asp-validation-for="StudioName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
                <a asp-action="Index" class="btn btn-outline-danger">Back to List</a>
            </div>
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}